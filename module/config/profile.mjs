/** Clés des flags génériques posés sur un profil — indépendants du psionique / des Points d'Ego. */
export const PROFILE_FLAGS = {
  /** Number : PV/niveau forcés pour ce profil, quelle que soit sa famille. Absent/0/invalide = pas de surcharge. */
  hpPerLevelOverride: "hpPerLevelOverride",
}
